# Project Goal
- Develop a math reasoning system.

# Virtual environment
- Always use virtual environment at: use alias "venv-python" which already encoded the path to the virtual environment.
- Just case the original path is needed: `/Users/zhenkun/Documents/Python/Virtual_environment/bin/python`